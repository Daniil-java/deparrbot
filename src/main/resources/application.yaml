server:
  port: 8081

bot:
  name: deparr_bot

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 1000000
        loggerLevel: basic

integrations:
  connect-timeout: 5000
  read-timeout: 1000000
  write-timeout: 5000
  openai-api:
    url: https://api.openai.com/v1/